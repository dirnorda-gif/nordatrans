# 🚀 Инструкция по деплою на NIC.RU

## 📋 Подготовка к деплою

### 1. Проверьте конфигурацию проекта

Убедитесь, что все API ключи и настройки правильные:

#### Яндекс.Метрика
Файл: `src/components/YandexMetrika.tsx`
```
Номер счетчика: 57594511
```

#### Bitrix24
Файл: `src/utils/bitrix24.ts`
```
Webhook URL: https://nordatrans.bitrix24.ru/rest/1/la8f9cfu3icpekuz/
```

#### Яндекс.Карты API
Файлы: 
- `src/components/YandexAddressInput.tsx`
- `src/components/ShippingCalculatorForm.tsx`

API ключ встроен в компоненты - проверьте актуальность.

---

## 🔨 Сборка проекта

### Шаг 1: Установка зависимостей (если ещё не установлены)

```bash
npm install
```

### Шаг 2: Сборка production билда

```bash
npm run build
```

Или используйте быстрый скрипт:

```bash
npm run deploy:prepare
```

Проект соберется в папку `dist/` (~2-3 минуты)

---

## 📂 Что будет в папке dist/

После сборки в папке `dist/` будет:
- `index.html` - главная страница
- `assets/` - JS и CSS файлы (минифицированные)
- `.htaccess` - правила для Apache
- `robots.txt` - для SEO
- Все изображения и иконки

**Размер билда: ~2-5 МБ** (зависит от изображений)

---

## 🌐 Деплой на хостинг NIC.RU

### Шаг 1: Подготовка хостинга

1. **Сделайте резервную копию WordPress сайта**
   - Войдите в панель управления NIC.RU
   - Перейдите в раздел "Хостинг" → "Файловый менеджер"
   - Скачайте все файлы из корневой папки
   - Или используйте FTP клиент (FileZilla, WinSCP)

2. **Удалите старые файлы WordPress**
   - Удалите все папки: `wp-admin`, `wp-content`, `wp-includes`
   - Удалите все файлы: `wp-config.php`, `index.php`, `.htaccess` и т.д.
   - **ВАЖНО**: Оставьте только если есть папки с важными файлами (например, загруженные документы)

### Шаг 2: Загрузка нового сайта

#### Вариант А: Через Файловый менеджер NIC.RU

1. Откройте **Файловый менеджер** в панели NIC.RU
2. Перейдите в **корневую папку** сайта (обычно это `/`)
3. **Загрузите ВСЕ файлы** из папки `dist/`:
   - Можно создать архив `dist.zip` и загрузить его
   - Затем распаковать на хостинге
   - **ВАЖНО**: Файлы должны быть в корне, не в папке `dist`!

4. Проверьте структуру - в корне должны быть:
   ```
   /
   ├── index.html
   ├── .htaccess
   ├── robots.txt
   ├── favicon.ico
   ├── assets/
   ├── logo_norda.webp
   └── ... (другие файлы)
   ```

#### Вариант Б: Через FTP/SFTP клиент

1. **Откройте FileZilla** (или любой FTP клиент)

2. **Подключитесь к серверу**:
   - **Хост**: обычно `ftp.nordatrans.ru` или указан в панели NIC.RU
   - **Пользователь**: логин от хостинга
   - **Пароль**: пароль от хостинга
   - **Порт**: 21 (FTP) или 22 (SFTP)

3. **Найдите данные для подключения** в панели NIC.RU:
   - Раздел "Хостинг" → "FTP доступ"
   - Там будут указаны: хост, логин, порт

4. **Загрузите файлы**:
   - Выберите ВСЕ файлы из папки `dist/`
   - Перетащите их в корневую папку на сервере
   - Дождитесь окончания загрузки

### Шаг 3: Проверка прав доступа

После загрузки проверьте права на файлы:

- **Файлы**: `644` (или `-rw-r--r--`)
- **Папки**: `755` (или `drwxr-xr-x`)
- **Особенно важно**: `.htaccess` должен иметь права `644`

В FileZilla: ПКМ на файл → "Атрибуты файла"

---

## ✅ Проверка работы сайта

### 1. Откройте сайт в браузере
```
https://nordatrans.ru/
```

### 2. Проверьте все страницы:
- ✅ Главная: `/`
- ✅ Тарифы: `/tarify`
- ✅ Тарифы из Москвы: `/tarify-iz-moskvy`
- ✅ Переезд: `/pereezd`
- ✅ Переезд из: `/pereezd-iz`
- ✅ Автопарк: `/fleet`
- ✅ Контакты: `/contacts`
- ✅ Спасибо: `/thanks`

### 3. Проверьте функционал:
- ✅ Калькулятор работает
- ✅ Аккордеон маршрутов загружается
- ✅ Формы отправляются в Bitrix24
- ✅ Яндекс.Метрика отслеживает посетителей

### 4. Проверьте мобильную версию
Откройте сайт на телефоне или через DevTools (F12 → Toggle Device Toolbar)

---

## 🔧 Возможные проблемы и решения

### Проблема 1: Страницы показывают 404
**Причина**: `.htaccess` не работает или отсутствует

**Решение**:
1. Проверьте, что файл `.htaccess` загружен в корень
2. Проверьте права на файл: `644`
3. Убедитесь, что на хостинге включен `mod_rewrite`
4. Свяжитесь с поддержкой NIC.RU для включения

### Проблема 2: Сайт не открывается
**Причина**: Файлы загружены не в ту папку

**Решение**:
1. Проверьте структуру: `index.html` должен быть в корне
2. Путь должен быть `/index.html`, а НЕ `/dist/index.html`

### Проблема 3: Изображения не отображаются
**Причина**: Неправильные пути или права доступа

**Решение**:
1. Проверьте, что папка `assets/` есть в корне
2. Проверьте права на папки: `755`
3. Проверьте права на файлы изображений: `644`

### Проблема 4: Стили не применяются
**Причина**: CSS файлы не загружаются

**Решение**:
1. Откройте DevTools (F12) → вкладка Network
2. Проверьте, какие файлы не загружаются
3. Проверьте права доступа на файлы в `assets/`
4. Очистите кэш браузера (Ctrl+Shift+R)

### Проблема 5: Формы не отправляются
**Причина**: CORS или неправильный Bitrix24 webhook

**Решение**:
1. Проверьте консоль браузера (F12) на ошибки
2. Убедитесь, что webhook Bitrix24 активен
3. Проверьте интернет-соединение сервера

---

## 📱 Как узнать тип веб-сервера на NIC.RU

### Способ 1: Через панель управления
1. Войдите в панель NIC.RU
2. Раздел "Хостинг" → "Информация"
3. Там будет указан тип сервера (Apache/Nginx)

### Способ 2: Через браузер (DevTools)
1. Откройте ваш текущий сайт https://nordatrans.ru/
2. Нажмите F12 (DevTools)
3. Перейдите на вкладку **Network**
4. Обновите страницу (F5)
5. Кликните на первый запрос (обычно это HTML документ)
6. Во вкладке **Headers** найдите строку **Server**
7. Там будет написано: `Apache` или `nginx`

**Скорее всего у вас Apache** - это стандарт для NIC.RU

---

## 🔄 Обновление сайта в будущем

Когда нужно обновить сайт:

1. Внесите изменения в код
2. Соберите проект: `npm run build`
3. Загрузите **только измененные файлы** из `dist/` на хостинг
4. Или замените все файлы целиком

**Совет**: Для быстрых обновлений используйте FTP клиент с синхронизацией

---

## 📞 Контакты поддержки

- **NIC.RU поддержка**: https://www.nic.ru/help/
- **Телефон**: +7 (495) 737-9222
- **Email**: support@nic.ru

---

## ✨ Чек-лист перед деплоем

- [ ] Резервная копия старого сайта сделана
- [ ] Проект собран: `npm run build`
- [ ] Файл `.htaccess` есть в `dist/`
- [ ] API ключи проверены (Метрика, Bitrix24, Карты)
- [ ] Референс-файл дизайна удален (807KB)
- [ ] Все файлы из `dist/` загружены в корень
- [ ] Права доступа проставлены: файлы `644`, папки `755`
- [ ] Все страницы открываются и работают
- [ ] Калькулятор и формы работают
- [ ] Метрика отслеживает посетителей
- [ ] Мобильная версия работает корректно

---

## 🎉 Готово!

Если все шаги выполнены, ваш сайт должен работать на https://nordatrans.ru/

**Время деплоя**: 15-30 минут (включая загрузку файлов)

Удачи! 🚀

