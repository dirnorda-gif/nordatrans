# Вертикальная укладка коробок — Документация

## 📦 Обзор

Добавлена поддержка вертикальной укладки коробок в конструкторе переезда. Теперь коробки могут размещаться:
- **На других коробках** (стопками)
- **На мебели** (диван, кровать, стол)
- **На полу** (если нет подходящих оснований)

Это позволяет значительно эффективнее использовать пространство кузова.

---

## 🎯 Алгоритм работы

### 1. Двухэтапная упаковка

**Этап 1: Размещение крупных предметов**
```
Мебель, техника, остальные предметы → MaxRects алгоритм (2D по полу)
```

**Этап 2: Вертикальная укладка коробок**
```
Коробки → Поиск оснований (мебель, другие коробки) → Размещение вертикально
```

---

### 2. Логика размещения коробок

Для каждой коробки алгоритм:

#### Шаг 1: Поиск основания
```typescript
for (каждый размещённый предмет) {
  if (предмет может служить основанием) {
    if (коробка влезает по габаритам) {
      if (коробка влезает по высоте кузова) {
        → Вычисляем оценку (score)
        → Запоминаем лучший вариант
      }
    }
  }
}
```

**Могут служить основанием:**
- ✅ Другие коробки
- ✅ Диваны
- ✅ Кровати
- ✅ Столы

**Не могут служить основанием:**
- ❌ Шкафы (узкие, неустойчивы)
- ❌ Холодильники (нельзя нагружать)
- ❌ Техника (стиральные машины, плиты и т.д.)

#### Шаг 2: Размещение на основании
```typescript
Коробка размещается:
- x, y = координаты основания (выравнивание по углу)
- z = высота верхней поверхности основания
- Проверка: z + высота_коробки ≤ высота_кузова
```

#### Шаг 3: Резервный вариант (пол)
```
Если не нашли основание → размещаем на полу (MaxRects)
```

---

## 🎨 Визуализация

### Обозначения

**Коробки на полу (z = 0):**
- Сплошная белая граница
- Непрозрачность: 100%
- Стандартная тень

**Коробки сверху (z > 0):**
- 🟡 Жёлтая пунктирная граница
- Непрозрачность: 85%
- Жёлтая подсветка (glow)
- Значок `↑↑` на предмете
- Подсказка: "📦 На высоте: X см"

### Пример визуализации

```
┌───────────────────────────────────┐
│ КАБИНА                            │
│ ┌──────────┐ ┌────┐ ┌────────┐  │
│ │  Диван   │ │📦↑↑│ │Кровать │  │ ← Коробки стоят сверху
│ │          │ └────┘ └────────┘  │
│ └──────────┘ ┌────┐              │
│              │📦  │              │ ← Коробка на полу
│              └────┘              │
│             ЗАДНИЕ ДВЕРИ         │
└───────────────────────────────────┘
```

---

## 📐 Координаты и размеры

### Система координат

```
      КАБИНА (x = 0)
         ↓
    ┌─────────────────┐
  Y │                 │ ← Правый борт (y = ширина)
  = │                 │
  0 │                 │
  → │                 │
    └─────────────────┘
      x = длина
         ↓
    ЗАДНИЕ ДВЕРИ
```

**PlacedItem:**
```typescript
{
  x: number,  // Позиция по длине (от кабины), см
  y: number,  // Позиция по ширине (от левого борта), см
  z: number,  // Высота от пола кузова, см (НОВОЕ!)
  width: number,   // Ширина в кузове
  height: number,  // Длина в кузове
  orientation: 0 | 1,
  item: MovingItem
}
```

---

## 🔧 Технические детали

### Функции определения

```typescript
// Можно ли ставить друг на друга?
function isStackable(item: MovingItem): boolean {
  return item.category === 'boxes';
}

// Может ли служить основанием?
function canSupportStack(item: MovingItem): boolean {
  return (
    item.category === 'boxes' ||
    (item.category === 'furniture' && (
      item.keywords.includes('диван') ||
      item.keywords.includes('кровать') ||
      item.keywords.includes('стол')
    ))
  );
}
```

### Оценка качества размещения

```typescript
score = потерянная_ширина 
      + потерянная_длина 
      + высота_основания * 0.1  // Приоритет ниже
```

**Цель:** минимизировать потери пространства, приоритет нижним слоям.

---

## 📊 Примеры

### Пример 1: 5 коробок + диван + кровать

**Было (без вертикальной укладки):**
```
Требуется: 3т машина (4.2м × 2.0м)
Коробки разложены Г-образно по полу
```

**Стало (с вертикальной укладкой):**
```
Требуется: Газель (3.0м × 1.7м)
Коробки размещены:
- 2 коробки на диване
- 2 коробки на кровати
- 1 коробка на полу
```

**Экономия:** `-30%` длины кузова

---

### Пример 2: 10 коробок (40×40×50 см)

**Без укладки:**
```
10 коробок × 0.08 м³ = 0.8 м³
Площадь пола: ~5.0 м² → Требуется 5т машина
```

**С укладкой:**
```
Стопка: 5 коробок высотой (5 × 50см = 250см)
2 стопки → 0.64 м² площади пола
→ Газель (3м × 1.7м = 5.1 м²)
```

**Экономия:** `-50%` площади пола

---

## ⚠️ Ограничения

### Высота кузова

```typescript
if (z + высота_коробки > высота_кузова) {
  // Не размещаем, ищем другое место
}
```

**Высоты кузовов:**
- Газель (500кг): 170 см
- 1.5т: 180 см
- 3т: 200 см
- 5т: 220 см
- 10т: 240 см
- 20т: 270 см

### Центр тяжести

⚠️ **Важно:** Алгоритм НЕ учитывает вес и центр тяжести. Тяжёлые предметы могут оказаться сверху.

**Рекомендация пользователю:** 
> "При размещении тяжёлых коробок вручную укладывайте их снизу"

---

## 🚀 Интеграция

### Вызов

```typescript
// Автоматически в MovingConstructor
const packingResult = calculatePackingHybrid(selectedItems);

// packingResult.placed содержит PlacedItem[] с координатой z
```

### Визуализация

```tsx
{placed.map(item => {
  const isStacked = item.z > 0;
  return (
    <div 
      className={isStacked ? 'border-yellow-400' : 'border-white'}
      style={{ opacity: isStacked ? 0.85 : 1 }}
    >
      {isStacked && <span>↑↑</span>}
    </div>
  );
})}
```

---

## 📈 Метрики эффективности

После укладки вычисляется:

```typescript
// Площадь занятого пола
const occupiedArea = placed.reduce((sum, item) => 
  sum + (item.width * item.height), 0
);

// Эффективность использования пола
const floorEfficiency = occupiedArea / totalFloorArea;

// Использование высоты (учитывая z-координату)
const maxUsedHeight = Math.max(...placed.map(
  item => item.z + item.item.height
));

const heightEfficiency = maxUsedHeight / truck.height;
```

---

## 🔄 Сравнение с предыдущей версией

| Параметр | Было (Shelf) | Стало (MaxRects + Stacking) |
|----------|--------------|------------------------------|
| Алгоритм 2D | Shelf (жёсткие полки) | MaxRects (свободные зоны) |
| Вертикальная укладка | ❌ Нет | ✅ Да (коробки) |
| Ориентация предметов | ✅ Да (ограниченно) | ✅ Да (оптимально) |
| Высокие предметы у стенок | ❌ Нет | ✅ Да (холодильники, шкафы) |
| Эффективность (тест) | 60-70% | 80-90% |

---

## 📝 История изменений

### v1.0 (текущая версия)
- ✅ Двухэтапная упаковка (крупное → коробки)
- ✅ Вертикальная укладка на мебель и другие коробки
- ✅ Визуализация с индикацией высоты
- ✅ Высокие предметы у стенок фургона
- ✅ MaxRects алгоритм для 2D размещения

### Планы на будущее
- 🔜 Учёт веса и центра тяжести
- 🔜 Ограничение высоты стопок (макс. 3 коробки)
- 🔜 Укладка техники друг на друга (если безопасно)
- 🔜 3D визуализация (вид сбоку)

---

## 🧪 Тестирование

### Как протестировать

1. Откройте конструктор переезда
2. Добавьте: диван, кровать, 5 коробок средних
3. Нажмите "🧪 Тест: Визуализация упаковки"
4. Проверьте:
   - Коробки с жёлтой границей (сверху)
   - Коробки с белой границей (на полу)
   - Подсказка показывает высоту

### Тестовые сценарии

**Сценарий 1: Стопки**
```
Груз: 10× коробка средняя (40×40×50)
Ожидание: 2 стопки по 5 коробок
Проверка: z = 0, 50, 100, 150, 200 см
```

**Сценарий 2: На мебели**
```
Груз: диван 3-местный, 3× коробка средняя
Ожидание: 2-3 коробки на диване
Проверка: z ≈ 85-90 см (высота дивана)
```

**Сценарий 3: Переполнение высоты**
```
Груз: 5× коробка средняя (стопка = 250см)
Кузов: Газель (высота 170см)
Ожидание: Только 3 коробки в стопке (150см), остальные на полу
```

---

**Версия:** 1.0  
**Дата:** 4 ноября 2025  
**Автор:** AI Assistant + Andrey

