Please analyze the current git status and staged changes. If this is the first commit in the branch (no previous commits), ask the user for a commit name/title. Then generate a concise but informative commit message that summarizes the main changes made. The message should follow conventional commit format and highlight the key modifications. After generating the message, show it to the user for confirmation before proceeding with the commit.