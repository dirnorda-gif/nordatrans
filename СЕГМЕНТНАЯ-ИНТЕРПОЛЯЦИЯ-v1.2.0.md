# 🎯 Сегментная интерполяция v1.2.0

**Дата внедрения:** 03.11.2025  
**Версия калькулятора:** 1.2.0

---

## 📋 Описание алгоритма

### Принцип работы

**Сегментная интерполяция** — это метод расчёта стоимости перевозки, при котором:

1. **Реперные точки** (стоимость полных машин) жёстко фиксированы
2. **Между точками** цена рассчитывается линейной интерполяцией
3. **Гарантируется** плавный рост цены без аномалий

---

## 🎯 Реперные точки

Реперные точки = стоимость полной загрузки каждого типа машины

| Объем | Машина | Формула | Пример (Москва → СПб) |
|-------|---------|---------|----------------------|
| **6 м³** | 1.5т (Газель) | тариф_1.5т × км | 33.9 × 700 = **23 730 ₽** |
| **12 м³** | 3т | тариф_3т × км | 53.8 × 700 = **37 660 ₽** |
| **20 м³** | 5т | тариф_5т × км | 72.9 × 700 = **51 030 ₽** |
| **40 м³** | 10т | тариф_10т × км | 72.9 × 700 = **51 030 ₽** |
| **80 м³** | 20т | тариф_20т × км | 94.5 × 700 = **66 150 ₽** |

---

## 📐 Формула интерполяции

### Для объёма между двумя реперными точками

```javascript
// Находим сегмент
if (volume > point1.volume && volume <= point2.volume) {
  // Вычисляем позицию в сегменте (0 to 1)
  ratio = (volume - point1.volume) / (point2.volume - point1.volume);
  
  // Линейная интерполяция
  cost = point1.cost + (point2.cost - point1.cost) × ratio;
}
```

### Пример расчёта: 9 м³ (Москва → СПб)

```
Сегмент: 6-12 м³
Point1: 6 м³ = 23 730 ₽
Point2: 12 м³ = 37 660 ₽

Ratio = (9 - 6) / (12 - 6) = 3 / 6 = 0.5

Cost = 23 730 + (37 660 - 23 730) × 0.5
     = 23 730 + 13 930 × 0.5
     = 23 730 + 6 965
     = 30 695 ₽
```

---

## 📊 Таблица стоимости: Москва → СПб

| Объем | Сегмент | Позиция | Стоимость | Реперная точка |
|-------|---------|---------|-----------|----------------|
| 6 м³ | — | — | **23 730 ₽** | ✅ |
| 7 м³ | 6-12 | 1/6 | **26 052 ₽** | |
| 8 м³ | 6-12 | 2/6 | **28 375 ₽** | |
| 9 м³ | 6-12 | 3/6 | **30 695 ₽** | |
| 10 м³ | 6-12 | 4/6 | **33 017 ₽** | |
| 11 м³ | 6-12 | 5/6 | **35 338 ₽** | |
| 12 м³ | — | — | **37 660 ₽** | ✅ |
| 13 м³ | 12-20 | 1/8 | **39 331 ₽** | |
| 15 м³ | 12-20 | 3/8 | **42 673 ₽** | |
| 18 м³ | 12-20 | 6/8 | **47 688 ₽** | |
| 20 м³ | — | — | **51 030 ₽** | ✅ |
| 25 м³ | 20-40 | 5/20 | **51 030 ₽** | Плато* |
| 30 м³ | 20-40 | 10/20 | **51 030 ₽** | Плато* |
| 40 м³ | — | — | **51 030 ₽** | ✅ |
| 50 м³ | 40-80 | 10/40 | **54 810 ₽** | |
| 60 м³ | 40-80 | 20/40 | **58 590 ₽** | |
| 70 м³ | 40-80 | 30/40 | **62 370 ₽** | |
| 80 м³ | — | — | **66 150 ₽** | ✅ |

\* *Плато 20-40 м³ — особенность маршрута Москва → СПб (тарифы 5т и 10т одинаковые)*

---

## ✅ Преимущества алгоритма

### 1. **100% совпадение реперных точек**
```
Калькулятор: 12 м³ = 37 660 ₽
Аккордеон:   12 м³ = 37 660 ₽
✅ СОВПАЛО!
```

### 2. **Плавный рост без аномалий**
```
7 м³  = 26 052 ₽
8 м³  = 28 375 ₽ (+9%)
9 м³  = 30 695 ₽ (+8%)
10 м³ = 33 017 ₽ (+8%)

✅ Монотонный рост
```

### 3. **Универсальность для всех маршрутов**
- Работает для любого маршрута из/в Москву
- Работает для маршрутов город-город
- Учитывает особенности тарифов (плато, если есть)

### 4. **Простота и прозрачность**
- Понятная логика клиентам
- Легко объяснить расчёт
- Нет сложных коэффициентов

---

## 🔧 Технические детали

### Код функции

```typescript
const calculateSegmentedCost = (
  volumeM3: number,
  weightKg: number,
  direction: "fromMoscow" | "toMoscow",
  targetCity: string,
  distanceKm: number
) => {
  // Реперные точки (динамически рассчитываются из тарифов)
  const referencePoints = [
    { volume: 6, cost: getTariff("1.5т") × distanceKm },
    { volume: 12, cost: getTariff("3т") × distanceKm },
    { volume: 20, cost: getTariff("5т") × distanceKm },
    { volume: 40, cost: getTariff("10т") × distanceKm },
    { volume: 80, cost: getTariff("20т") × distanceKm }
  ];
  
  // Находим сегмент
  for (let i = 0; i < referencePoints.length - 1; i++) {
    const p1 = referencePoints[i];
    const p2 = referencePoints[i + 1];
    
    if (volumeM3 <= p2.volume) {
      // Линейная интерполяция
      const ratio = (volumeM3 - p1.volume) / (p2.volume - p1.volume);
      return p1.cost + (p2.cost - p1.cost) × ratio;
    }
  }
}
```

### Обработка грузов >80 м³

```typescript
// Экстраполяция с доплатой 20%
const extraVolume = volumeM3 - 80;
const costPerExtraM3 = (cost_80m3 / 80) × 1.2;
cost = cost_80m3 + extraVolume × costPerExtraM3;
```

---

## 📈 График стоимости

```
₽
66,150 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 80м³ ✅
       ┃                               ╱
       ┃                          ╱
       ┃                     ╱
51,030 ┃━━━━━━━━━━━━━━━━━━━━ 20м³ ✅ 40м³ ✅
       ┃            ╱
37,660 ┃━━━━━━ 12м³ ✅
       ┃     ╱
23,730 ┃━ 6м³ ✅
       ┃
 7,500 ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
       0   10   20   30   40   50   60   70   80 м³

✅ = Реперная точка (100% совпадение)
━ = Плато (особенность тарифов)
╱ = Линейная интерполяция
```

---

## 🆚 Сравнение с предыдущими версиями

| Версия | Алгоритм | Реперные точки | Аномалии | Плато |
|--------|----------|----------------|----------|-------|
| 1.0.0 | Палетный | ❌ Не совпадают | ✅ Есть | ❌ |
| 1.1.0 | Пропорциональный | ✅ Совпадают | ✅ Есть | ❌ |
| **1.2.0** | **Сегментная интерполяция** | **✅ Совпадают** | **❌ Нет** | **✅ Учтено** |

---

## ⚠️ Особенности

### Плато на некоторых маршрутах

Если тарифы двух соседних категорий одинаковые (например, 5т и 10т):
```
20 м³ = 51 030 ₽
25 м³ = 51 030 ₽  ← Плато
30 м³ = 51 030 ₽  ← Плато
35 м³ = 51 030 ₽  ← Плато
40 м³ = 51 030 ₽
```

**Это не ошибка!** Это особенность реальных тарифов на данном маршруте.

---

## 🧪 Тестирование

### Проверка реперных точек

```javascript
// Для каждой реперной точки
const volumes = [6, 12, 20, 40, 80];

volumes.forEach(volume => {
  const calculated = calculateCost(volume);
  const expected = getTariff(category) × distance;
  
  assert(calculated === expected, "Реперная точка совпала");
});
```

### Проверка монотонности

```javascript
// Проверяем, что цена растёт
for (let v = 7; v <= 80; v++) {
  const cost1 = calculateCost(v);
  const cost2 = calculateCost(v + 1);
  
  assert(cost2 >= cost1, "Цена не убывает");
}
```

---

## 📝 Изменённые файлы

1. **src/utils/shippingCalculator.ts**
   - Добавлена функция `calculateSegmentedCost`
   - Обновлена версия: 1.1.0 → 1.2.0
   - Заменён вызов `calculateProportionalCost` на `calculateSegmentedCost`

---

## 🚀 Деплой

После внедрения этого изменения:

- ✅ **НЕ требуется** обновлять `routeCache.json` (реперные точки не изменились)
- ✅ Изменился только алгоритм интерполяции между точками
- ✅ Проект успешно собран и готов к деплою

---

**Версия документа:** 1.0  
**Дата:** 03.11.2025  
**Автор:** AI Assistant

