# 🚚 Калькулятор стоимости перевозки NORDA TRANS

## 📋 Описание

Полнофункциональный калькулятор для расчета стоимости перевозки грузов с учетом:
- Реального расстояния маршрута через Яндекс.Карты
- Веса и объема груза (автоматический выбор машины)
- Направления перевозки (в Москву / из Москвы / город-город)
- Минимальной стоимости перевозки (7 500 ₽)

## 🎯 Основные возможности

### ✅ Реализовано

1. **Умный расчет по весу и объему**
   - Автоматически выбирает большую машину из двух параметров
   - Решает проблему "тонны пуха" (легкий, но объемный груз)

2. **Три направления перевозки**
   - **Из Москвы** → дороже на 7%
   - **В Москву** → базовые тарифы
   - **Город-Город** → среднее значение

3. **Реальные тарифы**
   - 37 городов в направлении "Из Москвы"
   - 36 городов в направлении "В Москву"
   - Автоматический расчет отсутствующих тарифов через коэффициенты

4. **Минимальная стоимость**
   - Если расчет < 7 500 ₽, выводится минимальная цена
   - Показывается в отладочной информации

5. **Кэш маршрутов для аккордеона**
   - 15 популярных направлений
   - Предрассчитанные данные в JSON
   - Мгновенная загрузка без перерасчета

## 📂 Структура файлов

```
src/
├── utils/
│   └── shippingCalculator.ts    # Основная логика калькулятора
├── data/
│   └── routeCache.json          # Кэш предрассчитанных маршрутов
├── pages/
│   └── Index.tsx                # Интеграция калькулятора в UI
```

## 🔧 Как работает

### 1. Определение категории машины

Калькулятор анализирует **вес** и **объем** груза, затем выбирает **большую** категорию:

```typescript
Вес 500 кг + Объем 50 м³ → Машина 20т (объем требует большую машину)
Вес 15 000 кг + Объем 10 м³ → Машина 20т (вес требует большую машину)
```

#### Соответствие объема грузоподъемности:
- **46-82 м³** → 20 тонн
- **30-45 м³** → 10 тонн
- **15-30 м³** → 5 тонн
- **9-15 м³** → 3 тонны
- **6-9 м³** → 1,5 тонны
- **1-6 м³** → интерполяция

### 2. Расчет стоимости

```
Итоговая стоимость = Расстояние (км) × Тариф за км
```

Если тариф отсутствует (город есть, но нет данных для этого веса):
```
Тариф = Тариф_20т × Коэффициент
```

#### Коэффициенты "В Москву":
- 10 т: 0.76
- 5 т: 0.71
- 3 т: 0.31
- 1,5 т: 0.21
- 500 кг: 0.14

#### Коэффициенты "Из Москвы" (+7%):
- 10 т: 0.81
- 5 т: 0.76
- 3 т: 0.33
- 1,5 т: 0.22
- 500 кг: 0.15

### 3. Минимальная стоимость

Если `расчетная стоимость < 7 500 ₽`:
```typescript
Итоговая стоимость = 7 500 ₽
```

## 🔍 Отладочная информация

Открой **Консоль браузера** (F12 → Console) при использовании калькулятора.

Пример вывода:
```
┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃  🚚 РАСЧЕТ СТОИМОСТИ ПЕРЕВОЗКИ               ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
📍 Маршрут: Москва → Санкт-Петербург
📏 Расстояние: 704 км
🧭 Направление: Из Москвы
🏙️  Город для тарифа: Санкт-Петербург
⚖️  Вес груза: 5000 кг → 5 тонн
📦 Объем груза: 22 м³ → 5 тонн
🚛 Требуется машина: 5 тонн
💵 Тариф: 53.80 ₽/км
💰 ИТОГОВАЯ СТОИМОСТЬ: 37 875 ₽
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## 📊 Кэш маршрутов аккордеона

### Города в кэше:
1. Волгоград (971 км)
2. Екатеринбург (1660 км)
3. Казань (813 км)
4. Краснодар (1347 км)
5. Красноярск (4041 км)
6. Магнитогорск (1656 км) *
7. Нижний Новгород (434 км)
8. Новосибирск (3267 км)
9. Ростов-на-Дону (1078 км)
10. Самара (1090 км)
11. Санкт-Петербург (704 км)
12. Сочи (1624 км) *
13. Тверь (183 км)
14. Уфа (1327 км)
15. Челябинск (1919 км)

> **\*** Магнитогорск и Сочи используют тарифы ближайших городов (Челябинск и Краснодар)

### Структура кэша:

```json
{
  "version": "1.0.0",
  "generatedAt": "2025-10-27T12:00:00.000Z",
  "expiresAt": "2026-10-27T12:00:00.000Z",
  "routes": {
    "toMoscow": [...],
    "fromMoscow": [...]
  }
}
```

## 🔄 Обновление/Сброс кэша маршрутов

### Когда нужно обновлять кэш:

- Изменились тарифы в `shippingCalculator.ts`
- Изменились коэффициенты
- Добавились/удалились города
- Изменилась минимальная цена
- Изменилась логика расчета

### Способ 1: Ручное редактирование (быстро)

**Если нужно изменить несколько цен:**

1. Откройте файл `src/data/routeCache.json`
2. Найдите нужный маршрут и цену
3. Измените значение
4. Сохраните файл
5. Vite автоматически обновит страницу (HMR)

**Пример:**
```json
{
  "from": "Москва",
  "to": "Казань",
  "prices": [
    { "weight": "до 500 кг", "price": "10 875 ₽" }  // ← измените здесь
  ]
}
```

### Способ 2: Пересчет через калькулятор (рекомендуется)

**Если изменилась логика расчета:**

#### Шаг 1: Обновите версию в JSON

Откройте `src/data/routeCache.json` и измените версию:
```json
{
  "version": "1.0.1",  // ← увеличьте версию
  "generatedAt": "2025-10-28T10:00:00.000Z",
  "expiresAt": "2026-10-28T10:00:00.000Z",
  ...
}
```

#### Шаг 2: Пересчитайте цены вручную

Для каждого маршрута:
1. Откройте консоль браузера (F12)
2. Используйте калькулятор на сайте
3. Скопируйте результат из консоли
4. Обновите JSON

**Пример использования консоли:**
```javascript
// Вставьте в консоль браузера:
import { calculateShippingCost } from './src/utils/shippingCalculator.ts';

// Рассчитайте маршрут
const result = calculateShippingCost("Москва", "Казань", 813, 500, 2);
console.log(result.cost); // → скопируйте в JSON
```

#### Шаг 3: Автоматический пересчет (для продвинутых)

Создайте Node.js скрипт `scripts/updateCache.js`:

```javascript
// scripts/updateCache.js
import fs from 'fs';
import { calculateShippingCost } from '../src/utils/shippingCalculator.ts';

const routes = [
  { from: "Москва", to: "Казань", distance: 813 },
  // ... остальные маршруты
];

const weights = [
  { label: "до 500 кг", kg: 500, m3: 2 },
  { label: "1,5 т", kg: 1500, m3: 7 },
  { label: "3 т", kg: 3000, m3: 12 },
  { label: "5 т", kg: 5000, m3: 22 },
  { label: "10 т", kg: 10000, m3: 37 },
  { label: "20 т", kg: 20000, m3: 65 },
];

const cache = {
  version: "1.0.1",
  generatedAt: new Date().toISOString(),
  expiresAt: new Date(Date.now() + 365 * 24 * 60 * 60 * 1000).toISOString(),
  routes: {
    toMoscow: [],
    fromMoscow: []
  }
};

// Пересчитываем все маршруты
routes.forEach(route => {
  const prices = weights.map(w => {
    const result = calculateShippingCost(
      route.from, route.to, route.distance, w.kg, w.m3
    );
    return {
      weight: w.label,
      price: `${result.cost.toLocaleString()} ₽`
    };
  });

  cache.routes.toMoscow.push({
    from: route.from,
    to: route.to,
    distance: `${route.distance} км`,
    prices: prices
  });
});

// Сохраняем в файл
fs.writeFileSync(
  'src/data/routeCache.json',
  JSON.stringify(cache, null, 2)
);

console.log('✅ Кэш обновлен!');
```

**Запуск:**
```bash
node scripts/updateCache.js
```

### Способ 3: Полный сброс кэша (радикальный)

**Если нужно начать с нуля:**

1. Удалите файл `src/data/routeCache.json`
2. Создайте новый файл с базовой структурой:

```json
{
  "version": "1.0.0",
  "generatedAt": "2025-10-27T12:00:00.000Z",
  "expiresAt": "2026-10-27T12:00:00.000Z",
  "routes": {
    "toMoscow": [],
    "fromMoscow": []
  }
}
```

3. Заполните данные одним из способов выше

### Проверка обновления

После обновления кэша:

1. **Проверьте JSON валидность:**
   - Используйте онлайн валидатор: https://jsonlint.com/
   - Или в VSCode нажмите `Ctrl+Shift+P` → "Format Document"

2. **Проверьте в браузере:**
   - Обновите страницу (F5)
   - Откройте аккордеон
   - Убедитесь, что цены обновились

3. **Проверьте версию:**
   - Откройте консоль браузера
   - Введите: `console.log(routeCacheData.version)`
   - Должна быть новая версия

### Контрольный чеклист

При обновлении кэша проверьте:

- [ ] Версия увеличена
- [ ] Дата генерации актуальна
- [ ] Все 15 городов присутствуют
- [ ] У каждого маршрута 6 весовых категорий
- [ ] Все цены >= 7 500 ₽
- [ ] JSON валиден (нет ошибок синтаксиса)
- [ ] Файл сохранен с правильной кодировкой (UTF-8)

### Частые ошибки при обновлении

**Ошибка 1: Синтаксис JSON**
```json
❌ { "weight": "10 т": "117 740 ₽" }  // двоеточие вместо запятой
✅ { "weight": "10 т", "price": "117 740 ₽" }
```

**Ошибка 2: Лишняя запятая**
```json
❌ { "weight": "20 т", "price": "154 605 ₽" }, // лишняя запятая
   ]
✅ { "weight": "20 т", "price": "154 605 ₽" }
   ]
```

**Ошибка 3: Неправильная кодировка цен**
```json
❌ { "price": "7500" }  // без пробелов и валюты
✅ { "price": "7 500 ₽" }  // с пробелом-разделителем и рублем
```

### История версий кэша

Для отслеживания изменений ведите лог в комментариях:

```json
{
  "version": "1.0.1",
  "_changelog": [
    "1.0.1 - 2025-10-28 - Обновлены тарифы для Казани и Самары",
    "1.0.0 - 2025-10-27 - Начальная версия кэша"
  ],
  "routes": { ... }
}
```

## 🚀 API калькулятора

### Основная функция

```typescript
import { calculateShippingCost } from "@/utils/shippingCalculator";

const result = calculateShippingCost(
  fromCity: string,      // "Москва"
  toCity: string,        // "Санкт-Петербург"  
  distanceKm: number,    // 704
  weightKg: number,      // 5000
  volumeM3: number       // 22
);

// Результат:
{
  cost: 37875,                    // Итоговая стоимость
  costPerKm: 53.8,               // Тариф за км
  truckCapacity: "5т",           // Категория машины
  details: {
    direction: "Из Москвы",
    weightCategory: "5т",
    volumeCategory: "5т",
    finalCategory: "5т",
    distance: 704,
    ratePerKm: 53.8,
    cityUsed: "Санкт-Петербург",
    calculatedCost: 37875,       // До применения минимума
    minimumApplied: false        // Применен ли минимум 7500₽
  }
}
```

### Форматирование категории

```typescript
import { formatTruckCapacity } from "@/utils/shippingCalculator";

formatTruckCapacity("5т");  // "5 тонн"
formatTruckCapacity("1.5т"); // "1,5 тонны"
```

## 📝 TODO (для будущих улучшений)

- [ ] Добавить учет типа груза (хрупкий, опасный и т.д.)
- [ ] Реализовать сезонные коэффициенты
- [ ] Добавить калькуляцию времени погрузки/разгрузки
- [ ] Интеграция с CRM для сохранения расчетов
- [ ] Экспорт расчета в PDF

## 🐛 Известные ограничения

1. Магнитогорск и Сочи используют тарифы ближайших городов
2. Города не из базы тарифов вернут `null`
3. Направление город-город использует усредненные значения

## 📞 Контакты

При вопросах по калькулятору обращайтесь к разработчику.

---

**Версия:** 1.0.0  
**Дата:** 27.10.2025  
**Статус:** ✅ Готов к продакшену

