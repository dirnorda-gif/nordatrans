# ПОДРОБНОЕ ОПИСАНИЕ РЕАЛИЗАЦИИ ВАРИАНТА 4
## Линейная интерполяция для расчета стоимости груза 1-6 м³

---

## 1. КОНСТАНТЫ И ДАННЫЕ

### 1.1. Данные логиста для маршрута Москва → Ростов-на-Дону

```typescript
interface LogisticPricePoint {
  volumeM3: number;      // Объем в кубических метрах
  priceRub: number;       // Цена в рублях (без НДС)
}

const LOGISTIC_PRICE_POINTS_ROSTOV: LogisticPricePoint[] = [
  { volumeM3: 1, priceRub: 11000 },
  { volumeM3: 2, priceRub: 16000 },
  { volumeM3: 3, priceRub: 23000 },
  { volumeM3: 4, priceRub: 27000 },
  { volumeM3: 5, priceRub: 31000 },
  // volumeM3: 6 будет рассчитано отдельно (реперная точка)
];

// Реперная точка: 6 м³ = стоимость полной газели из калькулятора
const REFERENCE_POINT_6M3 = 37407; // ₽ (рассчитано: 1078 км × 34.7 ₽/км)
```

### 1.2. Следующая точка перехода (полная 3т машина)

```typescript
// Следующая точка после 6 м³: полная 3т машина (12 м³)
const NEXT_POINT_3T = {
  volumeM3: 12,
  priceRub: 50558, // ₽ (рассчитано: 1078 км × 46.9 ₽/км)
  tariffPerKm: 46.9, // ₽/км для категории 3т
};
```

### 1.3. Дополнительные константы

```typescript
// Константы для расчета
const MINIMUM_COST = 7500;              // Минимальная стоимость перевозки
const WEIGHT_TO_VOLUME_RATIO = 250;     // 250 кг = 1 м³ (логистический объем)
const TRUCK_1_5T_CAPACITY_M3 = 6;       // Вместимость газели (1.5т)
const TRUCK_3T_CAPACITY_M3 = 12;         // Вместимость 3т машины
```

---

## 2. СТРУКТУРА ДАННЫХ ДЛЯ ХРАНЕНИЯ

### 2.1. Конфигурация для каждого маршрута

```typescript
interface RouteLogisticConfig {
  // Идентификатор маршрута
  routeKey: string; // Например: "fromMoscow-Rostov-na-Donu"
  
  // Точки логиста для объемов 1-5 м³
  logisticPoints: LogisticPricePoint[];
  
  // Реперная точка 6 м³ (стоимость полной газели)
  referencePoint6m3: number;
  
  // Следующая точка (полная следующая категория машины)
  nextCategoryPoint: {
    volumeM3: number;
    priceRub: number;
    category: TruckCapacity; // "3т"
  };
  
  // Тариф за километр для газели (для расчета реперной точки)
  gazelleTariffPerKm: number; // 34.7 ₽/км
  
  // Расстояние маршрута
  distanceKm: number; // 1078 км
}

// Пример конфигурации для Москва → Ростов-на-Дону
const ROSTOV_ROUTE_CONFIG: RouteLogisticConfig = {
  routeKey: "fromMoscow-Rostov-na-Donu",
  logisticPoints: [
    { volumeM3: 1, priceRub: 11000 },
    { volumeM3: 2, priceRub: 16000 },
    { volumeM3: 3, priceRub: 23000 },
    { volumeM3: 4, priceRub: 27000 },
    { volumeM3: 5, priceRub: 31000 },
  ],
  referencePoint6m3: 37407,
  nextCategoryPoint: {
    volumeM3: 12,
    priceRub: 50558,
    category: "3т",
  },
  gazelleTariffPerKm: 34.7,
  distanceKm: 1078,
};
```

---

## 3. АЛГОРИТМ РАСЧЕТА

### 3.1. Функция линейной интерполяции

```typescript
/**
 * Линейная интерполяция между двумя точками
 * @param x1 - Объем первой точки
 * @param y1 - Цена первой точки
 * @param x2 - Объем второй точки
 * @param y2 - Цена второй точки
 * @param x - Текущий объем (между x1 и x2)
 * @returns Интерполированная цена
 */
function linearInterpolation(
  x1: number,
  y1: number,
  x2: number,
  y2: number,
  x: number
): number {
  // Предотвращаем деление на ноль
  if (x2 === x1) {
    return y1;
  }
  
  // Формула линейной интерполяции: y = y1 + (y2 - y1) * (x - x1) / (x2 - x1)
  const slope = (y2 - y1) / (x2 - x1);
  return y1 + slope * (x - x1);
}
```

### 3.2. Функция расчета стоимости для объемов 1-6 м³

```typescript
/**
 * Расчет стоимости для объемов 1-6 м³ с использованием данных логиста
 * @param volumeM3 - Объем груза в м³
 * @param config - Конфигурация маршрута
 * @returns Стоимость в рублях
 */
function calculateSmallVolumeCost(
  volumeM3: number,
  config: RouteLogisticConfig
): number {
  // Ограничиваем объем диапазоном 1-6 м³
  if (volumeM3 < 1) {
    return MINIMUM_COST;
  }
  if (volumeM3 > 6) {
    // Для объемов > 6 м³ используется другая логика
    throw new Error("Эта функция только для объемов ≤ 6 м³");
  }
  
  const { logisticPoints, referencePoint6m3 } = config;
  
  // Определяем сегмент для интерполяции
  // Сегменты:
  // - 1-2 м³: между точками (1, 11000) и (2, 16000)
  // - 2-3 м³: между точками (2, 16000) и (3, 23000)
  // - 3-4 м³: между точками (3, 23000) и (4, 27000)
  // - 4-5 м³: между точками (4, 27000) и (5, 31000)
  // - 5-6 м³: между точками (5, 31000) и (6, 37407)
  
  let result: number;
  
  if (volumeM3 === 1) {
    result = logisticPoints[0].priceRub; // 11,000 ₽
  } else if (volumeM3 <= 2) {
    // Сегмент 1-2 м³
    result = linearInterpolation(
      1, logisticPoints[0].priceRub,    // (1, 11000)
      2, logisticPoints[1].priceRub,    // (2, 16000)
      volumeM3
    );
  } else if (volumeM3 <= 3) {
    // Сегмент 2-3 м³
    result = linearInterpolation(
      2, logisticPoints[1].priceRub,    // (2, 16000)
      3, logisticPoints[2].priceRub,    // (3, 23000)
      volumeM3
    );
  } else if (volumeM3 <= 4) {
    // Сегмент 3-4 м³
    result = linearInterpolation(
      3, logisticPoints[2].priceRub,    // (3, 23000)
      4, logisticPoints[3].priceRub,    // (4, 27000)
      volumeM3
    );
  } else if (volumeM3 <= 5) {
    // Сегмент 4-5 м³
    result = linearInterpolation(
      4, logisticPoints[3].priceRub,    // (4, 27000)
      5, logisticPoints[4].priceRub,    // (5, 31000)
      volumeM3
    );
  } else if (volumeM3 <= 6) {
    // Сегмент 5-6 м³ (используем реперную точку)
    result = linearInterpolation(
      5, logisticPoints[4].priceRub,   // (5, 31000)
      6, referencePoint6m3,              // (6, 37407)
      volumeM3
    );
  } else {
    // Для ровно 6 м³ возвращаем реперную точку
    result = referencePoint6m3;
  }
  
  // Округляем до целого числа
  const rounded = Math.round(result);
  
  // Применяем минимальную стоимость
  return Math.max(rounded, MINIMUM_COST);
}
```

### 3.3. Функция расчета для объемов 7-12 м³ (переход к 3т)

```typescript
/**
 * Расчет стоимости для объемов 7-12 м³
 * Линейная интерполяция между 6 м³ и полной 3т машиной (12 м³)
 * @param volumeM3 - Объем груза в м³
 * @param config - Конфигурация маршрута
 * @returns Стоимость в рублях
 */
function calculateMediumVolumeCost(
  volumeM3: number,
  config: RouteLogisticConfig
): number {
  if (volumeM3 < 7 || volumeM3 > 12) {
    throw new Error("Эта функция только для объемов 7-12 м³");
  }
  
  const { referencePoint6m3, nextCategoryPoint } = config;
  
  // Линейная интерполяция между (6, 37407) и (12, 50558)
  const result = linearInterpolation(
    6, referencePoint6m3,                    // (6, 37407)
    nextCategoryPoint.volumeM3,            // (12, 50558)
    nextCategoryPoint.priceRub,
    volumeM3
  );
  
  return Math.max(Math.round(result), MINIMUM_COST);
}
```

### 3.4. Универсальная функция расчета (объединяет все сегменты)

```typescript
/**
 * Главная функция расчета стоимости для объемов 1-12 м³
 * @param volumeM3 - Объем груза в м³
 * @param config - Конфигурация маршрута
 * @returns Стоимость в рублях
 */
function calculateRostovCost(
  volumeM3: number,
  config: RouteLogisticConfig
): number {
  if (volumeM3 <= 6) {
    return calculateSmallVolumeCost(volumeM3, config);
  } else if (volumeM3 <= 12) {
    return calculateMediumVolumeCost(volumeM3, config);
  } else {
    // Для объемов > 12 м³ используем стандартную логику калькулятора
    // (пропорциональный расчет от полной машины следующей категории)
    throw new Error("Для объемов > 12 м³ используется стандартная логика");
  }
}
```

---

## 4. ПРИМЕРЫ РАСЧЕТОВ

### 4.1. Расчеты для целых значений объемов

| Объем | Расчет | Результат | Данные логиста | Разница |
|-------|--------|-----------|----------------|---------|
| 1 м³  | Прямое значение | 11,000 ₽ | 11,000 ₽ | 0 ₽ |
| 2 м³  | Прямое значение | 16,000 ₽ | 16,000 ₽ | 0 ₽ |
| 3 м³  | Прямое значение | 23,000 ₽ | 23,000 ₽ | 0 ₽ |
| 4 м³  | Прямое значение | 27,000 ₽ | 27,000 ₽ | 0 ₽ |
| 5 м³  | Прямое значение | 31,000 ₽ | 31,000 ₽ | 0 ₽ |
| 6 м³  | Реперная точка | 37,407 ₽ | 35-37,000 ₽ | В пределах |

### 4.2. Расчеты для дробных значений (примеры)

**1.5 м³:**
- Сегмент: 1-2 м³
- Интерполяция: `11000 + (16000 - 11000) × (1.5 - 1) / (2 - 1)`
- Формула: `11000 + 5000 × 0.5 = 11000 + 2500 = 13,500 ₽`

**2.5 м³:**
- Сегмент: 2-3 м³
- Интерполяция: `16000 + (23000 - 16000) × (2.5 - 2) / (3 - 2)`
- Формула: `16000 + 7000 × 0.5 = 16000 + 3500 = 19,500 ₽`

**4.7 м³:**
- Сегмент: 4-5 м³
- Интерполяция: `27000 + (31000 - 27000) × (4.7 - 4) / (5 - 4)`
- Формула: `27000 + 4000 × 0.7 = 27000 + 2800 = 29,800 ₽`

**7.5 м³:**
- Сегмент: 7-12 м³ (переход к 3т)
- Интерполяция: `37407 + (50558 - 37407) × (7.5 - 6) / (12 - 6)`
- Формула: `37407 + 13151 × 1.5 / 6 = 37407 + 3287.75 = 40,695 ₽`

**9 м³:**
- Сегмент: 7-12 м³
- Интерполяция: `37407 + (50558 - 37407) × (9 - 6) / (12 - 6)`
- Формула: `37407 + 13151 × 3 / 6 = 37407 + 6575.5 = 43,983 ₽`

---

## 5. ИНТЕГРАЦИЯ В СУЩЕСТВУЮЩИЙ КАЛЬКУЛЯТОР

### 5.1. Изменения в `shippingCalculator.ts`

```typescript
// Добавляем конфигурацию маршрутов
const ROUTE_LOGISTIC_CONFIGS: Record<string, RouteLogisticConfig> = {
  "fromMoscow-Rostov-na-Donu": ROSTOV_ROUTE_CONFIG,
  // В будущем можно добавить другие маршруты:
  // "fromMoscow-SPB": SPB_ROUTE_CONFIG,
  // "fromMoscow-Yaroslavl": YAROSLAVL_ROUTE_CONFIG,
};

// Модифицируем функцию calculateShippingCost
export const calculateShippingCost = (
  fromCity: string,
  toCity: string,
  distanceKm: number,
  weightKg: number,
  volumeM3: number
): CalculationResult | null => {
  // ... существующий код ...
  
  // Проверяем, есть ли специальная логика для этого маршрута
  const routeKey = `${direction}-${targetCity}`;
  const specialConfig = ROUTE_LOGISTIC_CONFIGS[routeKey];
  
  // Если есть специальная конфигурация и объем ≤ 12 м³
  if (specialConfig && volumeM3 <= 12) {
    const specialCost = calculateRostovCost(volumeM3, specialConfig);
    
    return {
      cost: specialCost,
      category: volumeM3 <= 6 ? "1.5т" : "3т",
      details: {
        // ... детали расчета ...
        isSpecialLogisticCalculation: true,
        routeKey: routeKey,
      },
    };
  }
  
  // Иначе используем стандартную логику
  // ... остальной код ...
};
```

---

## 6. ОБОСНОВАНИЕ ВЫБОРА ЗНАЧЕНИЙ

### 6.1. Почему данные логиста для 1-5 м³?

- **Эмпирические данные** - получены от логиста на основе реальных перевозок
- **Проверены практикой** - отражают реальную стоимость перевозки
- **Точные значения** - конкретные цены для каждого объема

### 6.2. Почему реперная точка 6 м³ = 37,407 ₽?

- **Математически точная** - рассчитана из тарифа: `1078 км × 34.7 ₽/км = 37,407 ₽`
- **Согласована с тарифной сеткой** - соответствует стоимости полной газели
- **Точка перехода** - между категориями 1.5т и 3т

### 6.3. Почему следующая точка 12 м³ = 50,558 ₽?

- **Полная машина следующей категории** - 3т машина (12 м³)
- **Рассчитана из тарифа** - `1078 км × 46.9 ₽/км = 50,558 ₽`
- **Обеспечивает плавный переход** - от газели к большим машинам

---

## 7. ПРЕИМУЩЕСТВА ТАКОГО ПОДХОДА

1. **Точность** - точное соответствие данным логиста для целых значений 1-5 м³
2. **Гибкость** - плавная интерполяция для дробных значений
3. **Масштабируемость** - легко добавить другие маршруты с их данными
4. **Прозрачность** - все значения явно заданы и понятны
5. **Совместимость** - легко интегрируется в существующий калькулятор
6. **Плавный переход** - нет скачков цены между сегментами

---

## 8. ВОЗМОЖНЫЕ УЛУЧШЕНИЯ В БУДУЩЕМ

1. **Кэширование результатов** - для ускорения расчетов
2. **Валидация данных** - проверка корректности конфигурации
3. **Логирование** - запись использованных значений для отладки
4. **Автоматическое обновление** - синхронизация с тарифной сеткой
5. **Расширение на другие маршруты** - применение логики к другим направлениям

---

## 9. ПРОВЕРКА РАБОТОСПОСОБНОСТИ

### Тестовые случаи:

```typescript
// Тест 1: Точные значения из данных логиста
calculateRostovCost(1, config) === 11000; // ✓
calculateRostovCost(2, config) === 16000; // ✓
calculateRostovCost(3, config) === 23000; // ✓
calculateRostovCost(4, config) === 27000; // ✓
calculateRostovCost(5, config) === 31000; // ✓
calculateRostovCost(6, config) === 37407; // ✓

// Тест 2: Реперная точка
calculateRostovCost(6, config) === config.referencePoint6m3; // ✓

// Тест 3: Переходная точка
calculateRostovCost(12, config) === config.nextCategoryPoint.priceRub; // ✓

// Тест 4: Плавность на границах сегментов
const price2 = calculateRostovCost(2, config);
const price2_01 = calculateRostovCost(2.01, config);
// price2_01 должна быть чуть больше price2 ✓
```

---

## ИТОГОВАЯ СТРУКТУРА КОНСТАНТ

```typescript
// Все константы в одном месте
const ROSTOV_LOGISTIC_CONFIG = {
  // Данные логиста
  points: [
    { vol: 1, price: 11000 },
    { vol: 2, price: 16000 },
    { vol: 3, price: 23000 },
    { vol: 4, price: 27000 },
    { vol: 5, price: 31000 },
  ],
  
  // Реперная точка
  ref6m3: 37407,
  
  // Следующая точка
  next: { vol: 12, price: 50558 },
  
  // Параметры маршрута
  distance: 1078,
  gazelleTariff: 34.7,
  
  // Константы
  minCost: 7500,
  weightRatio: 250, // кг/м³
};
```

